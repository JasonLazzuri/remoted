# Signaling Server Configuration

# Server Port
PORT=8080

# SSL/TLS Configuration (for WSS)
# Leave empty for development (uses WS)
# Required for production to enable secure WebSocket connections (wss://)
#
# To generate self-signed certificates for local testing:
# openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes
#
# For production (Railway, fly.io, etc.), SSL is typically handled by the platform
SSL_KEY_PATH=
SSL_CERT_PATH=

# Environment
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins
# Use * for development, specific domains for production
ALLOWED_ORIGINS=*
